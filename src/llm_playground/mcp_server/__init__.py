"""MCP server (Topic 3): expose the RAG retrievers as MCP tools.

See handlers.py for the pure tool logic and the "why MCP vs REST" writeup.
server.py wires those handlers into a real MCP server (FastMCP).
"""
